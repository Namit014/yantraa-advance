"use client";

import { SchematicsWorkspace } from "@/components/schematics/schematics-workspace";

export function SchematicsTab({ designData }: { designData?: any }) {
    return (
        <div className="w-full h-full">
            <SchematicsWorkspace designData={designData} />
        </div>
    );
}
